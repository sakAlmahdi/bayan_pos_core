///[promotion] =1
///[coupon] =2
///[selesDiscount] =3
///[clientDiscount] =4
///[system] = defulat if null

enum DiscountCategory {
  promotion,
  coupon,
  selesDiscount,
  clientDiscount,
  system,
}
